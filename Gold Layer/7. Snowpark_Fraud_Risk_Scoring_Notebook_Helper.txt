-- Step 6 — Validation Query

SELECT *
FROM GOLD_LAYER.CUSTOMER_FRAUD_RISK_SCORES
LIMIT 10;



SELECT
    RISK_CATEGORY,
    COUNT(*) AS TOTAL_CUSTOMERS
FROM GOLD_LAYER.CUSTOMER_FRAUD_RISK_SCORES
GROUP BY RISK_CATEGORY;